/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable the new app router if desired (Next.js 13+).
  experimental: {
    appDir: false
  }
};

module.exports = nextConfig;